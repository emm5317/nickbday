// TODO: Implement timeline event card with dot, tag badge, venue
import type { ScheduleEvent } from '@/data/schedule';

interface EventCardProps {
  event: ScheduleEvent;
  onPress: () => void;
}

export function EventCard(_props: EventCardProps) {
  return null;
}
