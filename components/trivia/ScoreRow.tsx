// TODO: Implement leaderboard player row with rank, name, score
interface ScoreRowProps {
  rank: number;
  name: string;
  score: number;
  isCurrentPlayer: boolean;
}

export function ScoreRow(_props: ScoreRowProps) {
  return null;
}
